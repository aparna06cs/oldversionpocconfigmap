spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: configmap-oldversionpoc
      reload:
        enabled: true
        mode: event
        strategy: refresh
endpoints:
  restart:
    enabled: true