spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: configmap-oldversionpoc
          - namespace: default
            name: configmap-oldversionpocpoc2
      reload:
        enabled: true
        mode: event
        strategy: refresh
        monitoringConfigMaps: true
endpoints:
  restart:
    enabled: true  refresh:    enabled: true
  info:    enabled: true
  env:    enabled: true